# Auth password hashes

This directory holds `users.hashed.json`, generated by:

```bash
node scripts/seed-auth-users.mjs
```

That file maps usernames to `scrypt:saltHex:hashHex` digests and is gitignored.
Never store plaintext passwords here or in `VITE_*` variables.
